name: Hello-world
on: [push]
jobs:
  execute-scritp: 
    runs-on: macos-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: run
        run: swift greeting.swift
